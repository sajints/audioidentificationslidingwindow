# from sklearn.metrics.pairwise import cosine_similarity
# import numpy as np
# from AudioVectorEmbedding import process_audio_to_embedding 

# # embedding1 = process_audio_to_embedding("audio1.wav")
# # embedding2 = process_audio_to_embedding("audio2.wav")
# emb1 = np.array(embedding1)
# emb2 = np.array(embedding2)

# similarity = cosine_similarity(
#     [embedding1],
#     [embedding2]
# )[0][0]

# print(f"Cosine similarity: {similarity:.4f}")